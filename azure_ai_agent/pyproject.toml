[project]
name = "azure-ai-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.0.0a1",
    "semantic-kernel[azure]>=1.24.1",
]
